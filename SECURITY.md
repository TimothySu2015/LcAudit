# 安全性政策

LcAudit 是一個要在**可能已被入侵的電腦**上執行的稽核工具。它本身就是攻擊者眼中的高價值目標 —— 一旦被動手腳，受害者會拿著一份「一切正常」的報告安心地繼續使用被控制的電腦。

因此本專案把自身的安全性當成功能的一部分看待。

## 回報漏洞

請寄到 **lcaudit2026@gmail.com**，標題註明 `[SECURITY]`。

如果你不方便具名，也可以用 GitHub 的 [Private vulnerability reporting](https://github.com/TimothySu2015/LcAudit/security/advisories/new)。

**請不要開公開 Issue**，直到修正版發佈為止。

收到後我會盡快回覆確認。這是一個人維護的免費專案，沒有 SLA，但**會叫使用者重灌電腦的工具出了問題，我會當成最優先的事處理**。

## 什麼算漏洞

以下都算，歡迎回報：

| 類型 | 為什麼嚴重 |
|---|---|
| **可以讓假紫P 通過 M1 檢查的手法** | 這是工具存在的理由。繞過了，使用者會被告知「你的紫P 是正版」而繼續被盜 |
| **會讓乾淨的電腦被判為高風險的誤報** | 工具最嚴重的失敗模式。誤報會讓正常使用者白白重灌，也會讓真正的警訊被當成雜訊忽略 |
| **HTML 報告的注入問題** | 報告內容大量來自攻擊者可控的資料（登入失敗事件裡的帳號名稱是嘗試者自己填的、檔名可以任意命名）。若跳脫有缺口，稽核報告會變成攻擊載體 |
| **違反唯讀承諾** | 任何在 `--output` 目錄之外的寫入 |
| **違反離線承諾** | 任何在檢查路徑上的網路請求。`--email` 上傳是唯一例外，且必須經使用者確認 |
| **會讓檢查靜默失敗的情況** | 「讀不到資料」被當成「沒有異常」比沒有這個檢查更糟 |
| **建置或發佈流程的供應鏈問題** | 見下方 |

## 已知且刻意的設計

回報前可以先看一下，以下不是漏洞：

- **執行檔未經程式碼簽章。** Azure Trusted Signing 目前限美加組織且需三年營運歷史，商業憑證對免費工具成本過高。替代的驗證方式見 README 的「你不必相信我」一節。
- **`ReportUploader` 裡的 `SharedToken` 是明文常數。** 它就在公開下載的執行檔裡，任何人都看得到。它不是存取控制，只用來擋隨機掃描的機器人。遭濫用時的處置是重新部署接收端換新網址。
- **執行檔中沒有任何憑證。** 這是刻意的 —— 若內嵌 Gmail 應用程式密碼，拿到的人不只能冒名寄信，還能讀取信箱裡所有已收到的受害者報告。
- **報告內含個人資訊**（電腦名稱、使用者帳號、來源 IP、程式路徑）。這是稽核報告的必要內容，工具會在送出前列出並要求確認，且**預設不送出**。

## 本專案自身的防護

| 措施 | 位置 |
|---|---|
| 禁用 API 的原始碼掃描測試 —— 出現即建置失敗 | `tests/LcAudit.Windows.Tests/ForbiddenApiGuardTests.cs` |
| 發佈前強制跑完整測試，失敗就不發佈 | `.github/workflows/release.yml` |
| 執行檔由 GitHub Actions 從乾淨環境建置，不經任何人的電腦 | 同上 |
| GitHub 建置來源證明（可用 `gh attestation verify` 驗證） | 同上 |
| 每次發佈公布 SHA-256 | 同上 |
| `TreatWarningsAsErrors=true` | `Directory.Build.props` |

**Release 頁面上的執行檔一律由 CI 產生，絕不手動上傳。** 若你發現某個版本的執行檔無法通過建置來源證明驗證，那件事本身就是最高等級的資安事件，請立刻回報。

## 支援的版本

只維護最新版。發現問題請先確認你用的是 [Releases](https://github.com/TimothySu2015/LcAudit/releases) 上的最新版本。

發佈後才發現問題時不會刪標籤重推，而是直接發下一個版本 —— 已下載的人拿到的是哪一版必須可追溯（報告裡的 `toolVersion` 就是靠這個對照）。
